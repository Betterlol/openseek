### 说明

本次修复解决的是在非交互终端模式下的 bug（继承 host-held 管道导致挂起），把子进程 stdin 从"写端永不关闭的共享管道"纠正为"立刻返回 EOF 的独立管道"——这才是非交互工具本应有的语义。

未来如果要支持"模型向子进程 stdin 写数据"（如 sort < <(echo ...) 的等价、对 stdin 交互的指令自动应答），那是功能拓展，需要：
1. 在 decode.mbt 新增 input 字段
2. spawn 时保持stdin写端打开
3. 写入模型提供的输入再关闭

本次不动它，保持范围集中在 bug 修复。