1. 思维链显示（特别是 GUI 中，避免硬刷新）
2. `文件/链接` 等颜色渲染
3. GUI 对 win 盘符改变不支持
4. `@` 引用文件只能引用文件，不能引用文件夹，建议支持引用文件夹。
5. TUI/GUI 不支持改模型？也是读取全局变量吗？
6. 用户对话不能复制和编辑。
7. 自动补齐 `/v1` 等
8. CLI `.exe` 无法启动
9. GUI 当前配置设计没有固有化：尤其是 `模型`，重新启动后会丢失，建议固有化配置。
