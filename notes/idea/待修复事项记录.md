
### GUI

1. 思维链显示（特别是 GUI 中，避免硬刷新）
2. GUI 对 win 盘符改变不支持
3. GUI 当前配置设计没有固有化：尤其是 `模型`，重新启动后会丢失，建议固有化配置。 — 已通过 localStorage 持久化

### 共有
1. markdown 渲染增强（特别对于 `文件/链接/工具` 的渲染）
2. `@` 引用文件只能引用文件，不能引用文件夹，建议支持引用文件夹。
3. TUI/GUI 不支持改模型？也是读取全局变量吗？ — GUI 已支持运行时切换+持久化
4. 用户对话不能复制和编辑。

### 其他
1. 自动补齐 `/v1` 等
2. CLI `.exe` 无法启动
